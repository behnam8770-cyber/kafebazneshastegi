const $=s=>document.querySelector(s);const nav=$('.nav'),menu=$('.menu');menu?.addEventListener('click',()=>{const open=nav.classList.toggle('open');menu.setAttribute('aria-expanded',open)});const sb=$('.search-button'),sp=$('.search'),close=$('.search-close');sb?.addEventListener('click',()=>{sp.classList.toggle('open');if(sp.classList.contains('open'))$('.search input')?.focus()});close?.addEventListener('click',()=>sp.classList.remove('open'));let big=false;$('#text-size')?.addEventListener('click',e=>{big=!big;document.documentElement.style.setProperty('--base',big?'21px':'19px');e.currentTarget.textContent=big?'A−':'A+'});const toast=$('.toast');function notify(t){if(!toast)return;toast.textContent=t;toast.classList.add('show');setTimeout(()=>toast.classList.remove('show'),2200)}document.querySelectorAll('[data-share]').forEach(b=>b.addEventListener('click',async()=>{const title=b.dataset.title||document.title,url=location.href;if(navigator.share){try{await navigator.share({title,text:title,url})}catch(e){}}else{await navigator.clipboard?.writeText(title+'\n'+url);notify('لینک مطلب کپی شد.')}}));document.querySelectorAll('[data-copy]').forEach(b=>b.addEventListener('click',async()=>{const t=$(b.dataset.copy);if(t){await navigator.clipboard?.writeText(t.value||t.textContent);notify('متن کپی شد.')}}));
function esc(s){return String(s||'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]))}function nl2p(s){return String(s||'').split(/\n{2,}/).filter(Boolean).map(x=>'<p>'+esc(x).replace(/\n/g,'<br>')+'</p>').join('')}function faDate(s){try{return new Intl.DateTimeFormat('fa-IR',{dateStyle:'medium'}).format(new Date(s))}catch(e){return s||''}}
async function loadCMS(){try{const r=await fetch('/api/posts');if(!r.ok)return;const posts=await r.json();if(!Array.isArray(posts)||!posts.length)return;const grid=document.querySelector('.post-grid');if(grid)grid.innerHTML=posts.map(p=>`<article class="post-card"><div class="thumb"${p.image_url?` style="background-image:url('${esc(p.image_url)}');background-size:cover;background-position:center"`:''}></div><div class="pad"><span class="eyebrow">${esc(p.category)}</span><h3>${esc(p.title)}</h3><p>${esc(p.excerpt)}</p><a href="article.html?slug=${encodeURIComponent(p.slug)}">ادامه مطلب ←</a></div></article>`).join('');const latest=document.querySelector('#latest .grid-feature');if(latest){const first=posts[0],rest=posts.slice(1,4);latest.innerHTML=`<article class="feature"><div class="visual"${first.image_url?` style="background-image:url('${esc(first.image_url)}');background-size:cover;background-position:center"`:''}><span>${esc(first.category)}</span></div><div class="body"><div class="meta">${faDate(first.published_at)} · کافه بازنشستگی</div><h3>${esc(first.title)}</h3><p>${esc(first.excerpt)}</p><div class="article-actions"><a href="article.html?slug=${encodeURIComponent(first.slug)}">ادامه مطلب ←</a></div></div></article><div class="mini-list">${rest.map(p=>`<article class="mini"><span class="tag">${esc(p.category)}</span><h3><a href="article.html?slug=${encodeURIComponent(p.slug)}">${esc(p.title)}</a></h3><p>${esc(p.excerpt)}</p></article>`).join('')}</div>`}const q=new URLSearchParams(location.search).get('slug'),article=document.querySelector('.article-page');if(article&&q){const p=posts.find(x=>x.slug===q);if(p){article.querySelector('.category').textContent=p.category;article.querySelector('h1').textContent=p.title;article.querySelector('.meta').textContent=`${faDate(p.published_at)} · نویسنده: کافه بازنشستگی`;article.querySelector('.lead').textContent=p.excerpt;article.querySelector('.content').innerHTML=nl2p(p.body);document.title=p.title+' | کافه بازنشستگی'}}}}catch(e){console.warn('CMS unavailable',e)}}loadCMS();
const title=$('#post-title'),lead=$('#post-lead'),body=$('#post-body'),out=$('#social-output');function makePack(){if(!out)return;const t=title?.value||'عنوان مطلب',l=lead?.value||'خلاصه کوتاه مطلب';out.textContent=`نسخه تلگرام:\n${t}\n\n${l}\n\nمطالعه کامل در کافه بازنشستگی:\n${location.origin}\n\nکپشن اینستاگرام:\n${t}\n\n${l}\n\n#کافه_بازنشستگی #بازنشستگی #زندگی\n\nمتن ایکس:\n${t} — ${l.slice(0,180)}\n\nنسخه شبکه‌های اجتماعی داخلی:\n${t}\n${l}`}['post-title','post-lead','post-body'].forEach(id=>$('#'+id)?.addEventListener('input',makePack));makePack();
